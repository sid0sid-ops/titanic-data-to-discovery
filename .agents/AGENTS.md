# Workspace Customization Rules

* **Always use the Google Colab CLI (`colab`) on Mac** for launching runtimes, running Python scripts, launching interactive shells/REPLs, opening consoles, listing active sessions, and managing runtime lifecycle states.
